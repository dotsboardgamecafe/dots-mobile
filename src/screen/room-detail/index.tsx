import { View, Text } from 'react-native'
import React from 'react'

const RoomDetail = (): React.ReactNode => {
	return (
		<View>
			<Text>RoomDetail</Text>
		</View>
	)
}

export default RoomDetail