import { View, Text } from 'react-native'
import React from 'react'

const GameDetail = (): React.ReactNode => {
	return (
		<View>
			<Text>GameDetail</Text>
		</View>
	)
}

export default GameDetail